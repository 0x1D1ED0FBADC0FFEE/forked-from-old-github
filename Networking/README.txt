HOW TO COMPILE AND RUN:

copy all files into a folder, open the terminal and type
"python3 tests.py". printouts are representations of the frames received by nodes. 


GROUP MEMBERS

A.S. (solo)

GIT: https://github.com/AlmondNMT/353Proj3.git (Currently private due to plagiarism warning, but WILL be published upon request.)

REPORT OF COMPLETION:

see DOCUMENTATION.doc or the pdf version of it.

KNOWN BUGS:

Frames are received and outputs written to files somewhat inconsistentlty, so the system could be more robust. The CCS and CAS shutdown properly, but I think the nodes are still in memory keeping the program open. 

